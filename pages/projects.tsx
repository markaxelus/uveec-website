import type { NextPage } from "next";
import { BaseLayout } from "../src/Layout/BaseLayout";

const Projects: NextPage = () => {
  return <BaseLayout>Projects</BaseLayout>;
};

export default Projects;
